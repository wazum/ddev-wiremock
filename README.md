# ddev-wiremock

WireMock as a DDEV add-on. Runs `wiremock/wiremock:3x` behind the DDEV router
with a small set of host commands that wrap the WireMock admin API.

Status: in development (pre-1.0).

<!-- filled in by Task 14 -->
